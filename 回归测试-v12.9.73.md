# 一人行 v12.9.73 回归测试步骤文档

> 对应版本：v12.9.73（versionCode 120973 / vphone1.14）
> 安装包：`一人行-手机专属版-v12.9.73+vphone1.14.apk`
> 测试前准备：**全新安装**（不是覆盖安装）——先卸载旧版再装，避免旧 SharedPreferences 残留干扰判定。

## 〇 · 沙箱已完成的验证（无需重复）

| 项目 | 结果 |
|---|---|
| `node --check` 全部改动 JS 语法 | 通过 |
| `node build.js`（单文件版 + Netlify 产物） | 通过（v12.9.73） |
| `gradlew assembleRelease`（Java 编译，含 Crash99/线程迁移/bridge.execute） | BUILD SUCCESSFUL |
| APK 签名 + 内含检查：capacitor.plugins.json 含 Crash99/Lock99/Lookus99，index.html 含 _crash99Boot/_perm99StatusP | 通过 |
| Netlify 线上部署 | ❌ credits 额度（不影响 APK） |

---

## 测试 ①：采集开关 → 进程被杀 → 不进入 Service 循环闪退（Bug1 回归）

**设备**：任一安卓真机（最好是被厂商省电策略杀得狠的：小米/华为/OV）。

1. 安装 App → 登录 → 独行空间 · 权限中心 → 授予「使用情况访问权限」。
2. 进入【此时此刻】→ 开启「电池状态」采集开关（Service 起来，通知栏出现常驻通知）。
3. 电脑连接手机，执行杀进程：
   ```bash
   adb shell am kill com.onexing.app        # 模拟后台被杀（保留 STICKY 重启）
   ```
   或无电脑时：开发者选项 → 开启「不保留活动」→ 把 App 划掉后台 → 等 10 秒。
4. **等待 30 秒**（观察期，系统会 STICKY 重启 Service）。
5. **预期（全部满足 = 通过）**：
   - 手机不出现闪退弹窗 / 不循环重启；
   - `adb logcat -s AndroidRuntime:E` 无 `Lookus99Service` 的 NPE 堆栈；
   - 重开 App →【此时此刻】→ 采集开关仍为开（持久化恢复）→ 实时卡数据继续更新；
   - 若采集开关全关状态被杀：Service 不再常驻（通知消失），也不闪退。
6. **补充断言（崩溃捕获联动）**：若期间发生任何崩溃，重开 App 2.5 秒内弹「🧯 检测到崩溃日志」征询弹窗。

## 测试 ②：Android 14+ 真机 → 开电池采集开关不抛 SecurityException（Bug2 回归）

**设备**：Android 14 / 15 / 16 真机（API ≥ 34，必须）。

1. 安装 App → 进入【此时此刻】。
2. 开启「电池状态」采集开关。
3. **预期**：
   - 不闪退；
   - `adb logcat -s AndroidRuntime:E | grep -i "RECEIVER"` 无 `One of RECEIVER_EXPORTED or RECEIVER_NOT_EXPORTED should be specified` / SecurityException；
   - 拔插充电器 →【此时此刻】实时卡电量/充电状态正确变化（广播真实收到）。
4. 再开关「使用情况访问」采集项一次（覆盖 screenRx 注册路径），同样不闪退。

## 测试 ③：锁屏状态 → 关「悬浮窗」开关 → 前台服务停机不闪退（Bug3 回归）

**设备**：任一安卓真机（Android 12+ 更佳——FgsTimeoutException 严格路径）。

1. 授予悬浮窗权限 + 使用情况访问权限。
2. 【应用数据】→ 应用锁机 → 勾选 1 个 App（如设置）→ 设 5 分钟 → 点【立即上锁】→ 切到被锁 App 确认屏保弹出（含倒计时）。
3. **按电源键锁屏**（保持锁屏状态）。
4. 解锁 → 迅速进入 独行空间 · 权限中心 → 关闭「悬浮窗权限」的 App 内开关。
5. **预期**：
   - 不闪退；屏保立刻销毁；锁机常驻通知消失（Service 停止）；
   - 切到被锁 App 不再有屏保；
   - `adb logcat -s AndroidRuntime:E` 无 `ForegroundServiceDidNotStartInTimeException`；
   - 重开权限中心悬浮窗开关 → 锁机页功能恢复可用。

## 补充测试 ④：系统设置手动撤销权限 → 资源正常释放

1. 开启全部采集（电池/使用情况/通知使用权/位置）+ 锁机在锁中。
2. 跳系统设置 → 手动撤销「悬浮窗权限」→ 返回 App：
   - 锁机页出现置灰门禁卡引导重新授权；已有屏保收起；无闪退。
3. 再撤销「使用情况访问」→ 返回 App：
   - 【此时此刻】实时卡提示需授权；【应用数据】使用时长页提示引导；无闪退。
4. 关闭「电池状态」采集开关：
   - `adb shell dumpsys power | grep -i "onexing:lookus99"` 无 WakeLock 残留持有（限时释放验证）。

## 补充测试 ⑤：崩溃捕获全链路（P1 新功能自证）

1. 制造一次崩溃（开发期可临时调用 `Crash99` 任意异常注入，或等待自然崩溃）。
2. 重启 App → 2.5 秒内弹征询弹窗（每批只弹一次：选「暂不」后重启 App 若无新崩溃则不再弹）。
3. 选「📋 复制」→ 剪贴板含 `time=…` 与堆栈文本。
4. 登录云账户后选「📤 上传云端」：
   - Supabase 已建 `crash99_logs` 表（建表 SQL：`id bigint generated always as identity primary key, user_id uuid, ver text, file text, log text`）→ 上传成功提示 + 本机日志清空；
   - 未建表 → 提示「上传未成功」且**本机日志保留**。
5. 隐私断言：抓包（ Charles / HttpCanary）确认**无任何自动上传请求**（只有用户点按钮后那一次）。

## 补充测试 ⑥：P2 性能验证（观察项，不设硬门槛）

1. **权限返回合并**：F12 / `adb logcat -s chromium:I | grep -c "checkAllPerms"` 从系统设置返回一次，原生调用次数 ≤ 3（旧版 5~7）。
2. **锁机线程迁移**：锁机运行中，开发者选项 GPU 呈现模式条无规律性主线程超帧；`adb shell dumpsys activity services com.onexing.app | grep -A2 Lock99Service` 可见 `Lock99Bg` 线程存在。
3. **心跳唯一性**：锁机页反复「保存规则」10 次 → logcat 中 `Lock99Service started` 后 tick 心跳频率仍为 1 秒 1 次（不出现 2 秒 2 次 = 多重循环回归）。

## 功能无降级回归（快速全量）

| 模块 | 验证点 | 结果 |
|---|---|---|
| 播放器/像素可视化 | 播放、暂停、切歌、可视化动画 | ☐ |
| 独行音乐五平台 + 漫游 | 搜索、歌单导入、漫游滑动 | ☐ |
| 此刻此刻 | 实时卡、时间轴、夜间统计 | ☐ |
| 应用锁机 | 上锁/解锁/阿福规则/历史日志 | ☐ |
| 语音（Vosk/TTS） | 识别、朗读 | ☐ |
| 云同步 | 登录、推拉、被踢 | ☐ |
| 数据中心/记录页/健康 | 各页渲染与写入 | ☐ |

---

**回归结论记录**：____（测试人 / 日期 / 通过与否，附 logcat 截图）
